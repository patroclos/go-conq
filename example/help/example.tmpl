`example` is a CLI app built with patroclos/go-conq.

{{.Root.Name}} [commands] [options] arguments
{{.Root.Name}} help [commands]

{{if .Root.Commands }}
Commands:
{{ range $cmd := .Root.Commands -}}
	- {{$cmd.Name }}
{{end}}
{{end}}
